<%= simple_form_for @picture, :html => {:multipart => true} do |f| %>

<%= f.file_field :image %>
<%= f.input :title %>
<%= f.input :remote_image_url, label: 'Image URL' %>
<%= f.button :submit %>
                 
<% end %>